const config = {
  pingChannel: 'jobs-pings',
  jobThreads: ['jobs-board', 'other-opportunities', 'non-swe-jobs'],
  jobPingRoles: ['jobs-intern', 'jobs-grad', 'jobs-junior', 'jobs-experienced']
};

export default config;
